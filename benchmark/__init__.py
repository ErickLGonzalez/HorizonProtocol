"""benchmark — self-contained comparison harnesses that sit alongside the
security stack without being part of it. Nothing under this package is a
gate; each subpackage produces an informational report, not a certificate.
"""
